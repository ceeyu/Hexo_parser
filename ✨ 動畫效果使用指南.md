# ✨ 動畫效果使用指南

## 🎨 概述

本主題集成了輕量級、高性能的動畫系統，靈感來自現代網站設計，同時保持極速載入。

### 特點

- ✅ **零依賴**：純原生 JavaScript 和 CSS
- ✅ **輕量級**：總大小 < 15KB
- ✅ **高性能**：使用 IntersectionObserver 和 requestAnimationFrame
- ✅ **響應式**：自動適配移動設備
- ✅ **無障礙**：尊重用戶的 `prefers-reduced-motion` 設置

## 📦 已集成的動畫

### 1. 滾動觸發動畫

元素進入視窗時自動觸發動畫。

**使用方法：**

```html
<!-- 淡入向上 -->
<div class="scroll-animate">內容</div>

<!-- 卡片動畫 -->
<div class="card-animate">卡片內容</div>
```

**可用類：**
- `.scroll-animate` - 基礎滾動動畫
- `.card-animate` - 卡片滾動動畫

### 2. 閱讀進度條

自動顯示文章閱讀進度（僅在文章頁）。

**特點：**
- 固定在頁面頂部
- 漸變色設計
- 平滑動畫

### 3. 懸停效果

**可用類：**

```html
<!-- 上浮效果 -->
<div class="hover-lift">懸停時上浮</div>

<!-- 縮放效果 -->
<div class="hover-scale">懸停時放大</div>

<!-- 發光效果 -->
<button class="hover-glow">懸停發光</button>

<!-- 波紋效果 -->
<button class="ripple">點擊波紋</button>
```

### 4. 淡入動畫

**可用類：**

```html
<!-- 基礎淡入 -->
<div class="animate-fade-in">淡入</div>

<!-- 淡入向上 -->
<div class="animate-fade-in-up">淡入向上</div>

<!-- 淡入從左 -->
<div class="animate-fade-in-left">從左淡入</div>

<!-- 淡入從右 -->
<div class="animate-fade-in-right">從右淡入</div>

<!-- 縮放淡入 -->
<div class="animate-scale-in">縮放淡入</div>

<!-- 滑入向上 -->
<div class="animate-slide-in-up">滑入向上</div>
```

### 5. 延遲動畫

為動畫添加延遲，創建序列效果。

```html
<div class="animate-fade-in delay-1">第一個</div>
<div class="animate-fade-in delay-2">第二個</div>
<div class="animate-fade-in delay-3">第三個</div>
```

**可用延遲：**
- `.delay-1` - 0.1s
- `.delay-2` - 0.2s
- `.delay-3` - 0.3s
- `.delay-4` - 0.4s
- `.delay-5` - 0.5s

### 6. 文字效果

```html
<!-- 漸變文字 -->
<h1 class="text-gradient">漸變標題</h1>
```

### 7. 視差效果

```html
<!-- 視差滾動 -->
<div class="parallax" data-parallax-speed="0.5">
  視差內容
</div>
```

**速度值：**
- `0.3` - 慢速
- `0.5` - 中速（默認）
- `0.8` - 快速

## 🔧 JavaScript API

### ScrollAnimator

滾動動畫觀察器。

```javascript
// 使用默認設置
const animator = new AnimationUtils.ScrollAnimator();

// 自定義設置
const animator = new AnimationUtils.ScrollAnimator({
  threshold: 0.2,
  rootMargin: '0px 0px -100px 0px',
  animateClass: 'custom-visible'
});
```

### ReadingProgress

閱讀進度條。

```javascript
const progress = new AnimationUtils.ReadingProgress();
```

### ParallaxEffect

視差效果。

```javascript
const parallax = new AnimationUtils.ParallaxEffect();
```

### TypeWriter

打字機效果。

```javascript
const element = document.querySelector('.typing-text');
const typewriter = new AnimationUtils.TypeWriter(element, {
  speed: 50,    // 打字速度（毫秒）
  delay: 1000   // 開始前延遲
});
typewriter.start();
```

### 數字計數動畫

```javascript
const element = document.querySelector('.counter');
AnimationUtils.animateNumber(element, 1000, 2000);
// 從 0 計數到 1000，持續 2 秒
```

### 淡入淡出

```javascript
const element = document.querySelector('.fade-element');

// 淡入
AnimationUtils.fadeIn(element, 300);

// 淡出
AnimationUtils.fadeOut(element, 300);

// 切換
AnimationUtils.fadeToggle(element, 300);
```

## 🎯 實際應用範例

### 範例 1：文章列表動畫

```html
<article class="post card-animate hover-lift">
  <h2 class="post-title animate-fade-in-up">文章標題</h2>
  <div class="post-content scroll-animate">
    文章內容...
  </div>
</article>
```

### 範例 2：英雄區塊

```html
<section class="hero">
  <h1 class="animate-fade-in-up delay-1 text-gradient">
    歡迎來到我的博客
  </h1>
  <p class="animate-fade-in-up delay-2">
    分享技術與生活
  </p>
  <button class="animate-fade-in-up delay-3 hover-glow ripple">
    開始閱讀
  </button>
</section>
```

### 範例 3：統計數字

```html
<div class="stats">
  <div class="stat-item scroll-animate">
    <span class="counter" data-target="90">0</span>
    <p>篇文章</p>
  </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const counters = document.querySelectorAll('.counter');
  counters.forEach(counter => {
    const target = parseInt(counter.dataset.target);
    AnimationUtils.animateNumber(counter, target, 2000);
  });
});
</script>
```

### 範例 4：視差背景

```html
<section class="hero" style="position: relative; overflow: hidden;">
  <div class="parallax" data-parallax-speed="0.3" 
       style="position: absolute; top: 0; left: 0; width: 100%; height: 100%;">
    <img src="background.jpg" alt="背景">
  </div>
  <div style="position: relative; z-index: 1;">
    <h1>內容在前景</h1>
  </div>
</section>
```

## ⚡ 性能優化建議

### 1. 使用 will-change

為即將動畫的元素添加 `will-change`：

```css
.will-animate {
  will-change: transform, opacity;
}
```

**注意：** 不要過度使用，只在動畫開始前添加，結束後移除。

### 2. GPU 加速

```css
.gpu-accelerate {
  transform: translateZ(0);
  backface-visibility: hidden;
}
```

### 3. 減少重繪

- 優先使用 `transform` 和 `opacity`
- 避免動畫 `width`、`height`、`top`、`left` 等屬性

### 4. 使用 requestAnimationFrame

所有動畫都已使用 `requestAnimationFrame` 優化。

### 5. 延遲加載

動畫腳本使用 `defer` 屬性延遲加載：

```html
<script defer src="/js/animations.js"></script>
```

## 🎨 自定義動畫

### 創建自定義動畫

在 `animations.css` 中添加：

```css
@keyframes customAnimation {
  from {
    opacity: 0;
    transform: rotate(0deg);
  }
  to {
    opacity: 1;
    transform: rotate(360deg);
  }
}

.animate-custom {
  animation: customAnimation 1s ease-in-out both;
}
```

### 修改動畫參數

在 `:root` 中修改全局變數：

```css
:root {
  --animation-duration: 0.8s;  /* 動畫持續時間 */
  --animation-easing: cubic-bezier(0.4, 0, 0.2, 1);  /* 緩動函數 */
  --animation-delay: 0.15s;    /* 延遲時間 */
}
```

## 🌐 瀏覽器支持

- ✅ Chrome 51+
- ✅ Firefox 55+
- ✅ Safari 12.1+
- ✅ Edge 15+

**降級處理：**
- 不支持 IntersectionObserver 的瀏覽器會直接顯示所有元素
- 用戶設置 `prefers-reduced-motion` 時會禁用所有動畫

## 📊 性能指標

### 文件大小

- `animations.css`: ~8KB (壓縮後 ~2KB)
- `animations.js`: ~6KB (壓縮後 ~2KB)
- **總計**: ~14KB (壓縮後 ~4KB)

### 性能影響

- **首屏渲染**: 無影響（異步加載）
- **滾動性能**: 60 FPS（使用 IntersectionObserver）
- **內存佔用**: < 1MB

## 🔗 參考資源

### 靈感來源

- [jesse-zhou.com](https://www.jesse-zhou.com/) - 現代網站設計
- [AOS](https://github.com/michalsnik/aos) - 26.8k stars
- [Anime.js](https://animejs.com/) - 49k+ stars

### 學習資源

- [MDN - Web Animations API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Animations_API)
- [CSS Tricks - Animation](https://css-tricks.com/almanac/properties/a/animation/)
- [Google Developers - Animations](https://developers.google.com/web/fundamentals/design-and-ux/animations)

## 🐛 故障排除

### 動畫不工作

1. 檢查瀏覽器控制台是否有錯誤
2. 確認 CSS 和 JS 文件已正確載入
3. 檢查元素是否有正確的類名
4. 確認用戶沒有啟用 `prefers-reduced-motion`

### 性能問題

1. 減少同時動畫的元素數量
2. 使用 `will-change` 優化
3. 檢查是否有過多的重繪
4. 考慮禁用視差效果（在移動設備上）

### 移動設備問題

某些動畫在移動設備上可能不流暢，可以使用媒體查詢禁用：

```css
@media (max-width: 768px) {
  .parallax {
    transform: none !important;
  }
}
```

## 💡 最佳實踐

1. **適度使用**：不要過度使用動畫，保持簡潔
2. **性能優先**：始終測試性能影響
3. **無障礙**：確保動畫不影響可訪問性
4. **漸進增強**：確保沒有動畫時內容仍可用
5. **測試**：在不同設備和瀏覽器上測試

---

**提示**：動畫應該增強用戶體驗，而不是分散注意力。保持簡單、流暢、有目的。
